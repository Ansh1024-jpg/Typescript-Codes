const superheros: string[] = [];
superheros.push("Spiderman");

const heroPoewer: Array<number> = [];

type User = {
    name: string
    isActive: boolean
}

const allUsers: User[] = [];
allUsers.push({name: 'Alice', isActive: false})

const MLModels: number[][] = [  //Array os arrays
    [255, 255, 255],
    [0,0,0]
]